% Font
\usepackage{newcent}

% Colors (safe)
\usepackage{xcolor}
\definecolor{FORRTblue}{HTML}{2C3E50}
% (Avoid \pagecolor to eliminate any chance of a driver-induced first blank page)
% \pagecolor{white}

% Heading colours
\addtokomafont{section}{\color{FORRTblue}}
\addtokomafont{subsection}{\color{FORRTblue}}

% Links
\AtBeginDocument{
  \hypersetup{
    colorlinks=true,
    linkcolor=FORRTblue,
    urlcolor=FORRTblue,
    citecolor=FORRTblue
  }
}

% Line spacing
\usepackage{setspace}
\setstretch{1.5}

% Smaller tables
\usepackage{etoolbox}
\AtBeginEnvironment{longtable}{\small}
\AtBeginEnvironment{tabular}{\small}

% Disable auto title
\makeatletter
\renewcommand{\maketitle}{}%
\makeatother

% Avoid forced blank recto pages
\makeatletter
\let\cleardoublepage\clearpage
\makeatother

% TOC layout & page building
\KOMAoptions{toc=graduated}
\flushbottom

% (Optional) tidy ToC depth
\setcounter{tocdepth}{2}
